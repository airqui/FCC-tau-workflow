# Integration-test fixture policy

No ROOT or EDM4hep binary is stored in Git. A future smoke test will resolve a checksum-pinned external one-event REC through a manifest in `tests/fixtures/manifests/`.
